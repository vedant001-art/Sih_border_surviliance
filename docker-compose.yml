version: '3.8'

services:
  bordersec-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bordersec_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
